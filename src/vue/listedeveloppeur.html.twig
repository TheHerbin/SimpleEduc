{% extends "base.html.twig" %}
{% block title %}
	{{ parent() }}
	Liste des développeurs
{% endblock %}
{% block contenu %}
	{{ parent() }}
	
		<div class="container">
	<div class="table-responsive">
	{% if form.etat is defined %}
{% if form.etat == true %}
<h1 class="h3 mb-3 font-weight-normal">Suppression réussie</h1>
{% else %}
<h1 class="alert alert-danger" role="alert">Problème d'insertion</h1>
{% endif %}
{% endif %}
		<table class="table">
			<thead>
				<tr>
					<th scope="col">Nom</th>
					<th scope="col">Prenom</th>
					<th scope="col">Indice de rémunération</th>
					<th scope="col">Coût Horaire</th>
					<th scope="col"></th>
					
				</tr>
			</thead>
			<tbody>
				{% for u in liste %}


					<tr>
						<td>{{u.nom}}</td>
						<td>{{u.prenom}}</td>
						<td>{{u.indiceremuneration}}</td>
						<td>{{u.couthoraire}}</td>
						
						<td>	<a <span href="index.php?page=modifdeveloppeur&id={{u.id}}" class="material-icons">edit</span>
                        <a <span href="index.php?page=listedeveloppeur&id={{u.id}}" class="material-icons">clear</span></td>
					
						
					</tr>
                    {% endfor %}
 </tbody>
 </table>
 </div>
  </div>


				{% endblock %}
